# goit-pycore-hw-06

# Тема 9. Домашня робота — Основи роботи з класами

## 📂 Структура

````text
goit-pycore-hw-06/
├── address_book.py   # Field, Name, Phone, Record, AddressBook
├── demo.py           # Приклад використання (з умови)
├── .gitignore
└── README.md


## ▶️ Запуск

**Вимога:** стандартний Python (перевірено на 3.12), зовнішні пакети не потрібні.

**Windows / VS Code Terminal / PowerShell**

```bash
py demo.py
````

Очікувано побачите:

створення записів John/Jane,

додавання телефонів,

редагування телефону John,

вивід усіх записів,

пошук конкретного телефону,

видалення запису Jane.

🧠 Короткий опис класів

Field — базове поле (зберігає value, надає **str**/**repr**).

Name(Field) — обов’язкове поле імені (порожній рядок заборонено).

Phone(Field) — номер з валідацією рівно 10 цифр; під час присвоєння нормалізує значення (залишає лише цифри). Некоректний формат ⇒ ValueError.

Record — один контакт: name: Name + список phones: list[Phone]. Методи:

add_phone(str), remove_phone(str), edit_phone(old, new),

find_phone(str) — повертає Phone | None.

AddressBook(UserDict) — колекція записів:

add_record(record), find(name) -> Record | None, delete(name) -> bool.

✅ Відповідність вимогам

AddressBook: реалізовано add_record, find, delete.

Record: містить Name і список Phone; реалізовано add_phone, remove_phone, edit_phone, find_phone.

Phone: валідація на 10 цифр (і нормалізація вводу).
